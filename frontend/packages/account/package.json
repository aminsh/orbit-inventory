{
  "name": "@orbit/account",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "@orbit/core": "1.0.0"
  },
  "devDependencies": {},
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview",
    "tailwind:watch": "npx tailwindcss -i ./src/styles/index.css -o ./src/output.css --watch"
  }
}
